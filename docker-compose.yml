services:

  # ── Django Admin Panel ─────────────────────────────────────────────────────
  web:
    build: .
    container_name: barb_web
    entrypoint: /entrypoint.sh web
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./:/app
    restart: unless-stopped
    depends_on: []

  # ── Telegram Bot ───────────────────────────────────────────────────────────
  bot:
    build: .
    container_name: barb_bot
    entrypoint: /entrypoint.sh bot
    env_file:
      - .env
    volumes:
      - ./:/app
    restart: unless-stopped
    depends_on:
      - web

  # ── React Frontend ─────────────────────────────────────────────────────────
  frontend:
    build: ./frontend
    container_name: barb_frontend
    ports:
      - "5173:80"
    restart: unless-stopped
    depends_on:
      - web
